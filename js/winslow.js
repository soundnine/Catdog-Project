import {Characters} from './characters.js'
export class Winslow extends Characters{
    constructor(image, radius, width, height, xSpeed, ySpeed){
        super(image, radius, width, height, xSpeed, ySpeed);
    }
}